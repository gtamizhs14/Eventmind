apiVersion: v1
kind: ConfigMap
metadata:
  name: eventmind-config
  namespace: eventmind
data:
  PORT: "8080"
  ENV: "production"
  LOG_LEVEL: "info"
  METRICS_PORT: "9091"
  KAFKA_GROUP_ID: "eventmind-consumer"
  KAFKA_TOPIC_EVENTS: "events"
  KAFKA_TOPIC_DLQ: "events.dlq"
  KAFKA_USE_TLS: "true"
  MONGODB_DATABASE: "eventmind"
  RETRY_MAX_ATTEMPTS: "5"
  RETRY_BASE_DELAY_MS: "1000"
