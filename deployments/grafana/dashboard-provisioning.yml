apiVersion: 1

providers:
  - name: eventmind
    orgId: 1
    folder: EventMind
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
